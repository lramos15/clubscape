# M1 blocked checkpoint

**The first milestone is incomplete and has not reached acceptance.**
The complete Section 30 journey remains the approved scope. Zero of its 28
recorded acceptance gates are verified. No later milestone, early-access
release, production deployment or desktop deferral is approved or claimed.

## Preserved revision and completed infrastructure

- Execution branch: `milestone/01-starter-journey`; `main` and `origin/main`
  were not changed or pushed.
- Verified integrated infrastructure revision:
  **`275d3cc83d6a7c2118d0afcf82490f364e5d2986`**. Subsequent checkpoint changes
  only record documentation, task state and evidence; they do not claim a
  completed game candidate.
- Working PostgreSQL account creation/authentication, Argon2id passwords,
  expiring/revocable digest-at-rest sessions, concurrent uniqueness/session
  limits, generated native/WASM-compatible Protobuf, and a native account
  lifecycle client using the same real service.
- Explicit unavailable-gameplay/character state: no tutorial skips, seeded
  character, item grants or fake persistent game progress were introduced.
- Isolated local Compose/bootstrap commands, CPU/memory-bounded disposable
  database checks, structured errors/logging, source-input verification,
  checkpoint regression checks, CODEOWNERS and infrastructure CI configuration.
- Reference identity fixed to OSRS build 240/OpenRS2 cache 2695, with pinned
  wiki references and actual verified master/map-index bytes. The current
  tutorial reference is Learning the Ropes; exact source-state gaps remain
  visible rather than adopting a remembered historical tutorial.

## Validation against that infrastructure

| Command / observation | Result and boundary |
| --- | --- |
| `just bootstrap` | Passed; 30 Rust unit/transport checks. Builds account infrastructure, not content or a renderer. |
| `just lint` | Formatting, workspace Clippy with warnings denied, and checkpoint structure passed. |
| `just check-wasm` | Shared protocol compiled for WASM. No graphical client is established. |
| `python3 -m unittest discover -s tests/workflow -q` | 31 Python regressions passed. |
| `python3 tools/dev.py integration --report milestones/evidence/m1-account-foundations.json` | Nine real PostgreSQL/HTTP tests and independent native account lifecycle passed, including actual server process restart and stalled-transport recovery. |
| `just server`, real `/healthz`, `just sim` | Local development service responsive; synthetic account persisted; no active session left. |
| `just db-stop` / `just db-start` | One synthetic account and zero sessions survived the development database restart. Database stopped afterward. |
| `just reference-verify` | Two original index inputs matched their pre-established hashes. Not content, asset, gameplay or visual coverage. |
| `just reference-runtime-fetch` / `just reference-runtime-inspect` | Two original artifacts hash-verified and bytecode inspected without executing the reference client. Not captures or compatibility. |
| `python3 tools/milestone.py check` | Correctly exited 1: incomplete M1, missing owner approvals, zero verified gates; no successor authorized. |

Persistent machine-readable evidence:
[`m1-account-foundations.json`](../../milestones/evidence/m1-account-foundations.json)
and [`m1-local-bootstrap.json`](../../milestones/evidence/m1-local-bootstrap.json).
The live integration report records a clean source worktree and exact lockfile
hash at execution. Hosted GitHub CI has not run; local checks do not claim that
coverage.

Independent account review identified an unlimited wait on acquired but stalled
database connections. The repair added database/migration deadlines, bounded
command/drain/cleanup behavior and three real stalled-response regressions.
A second scoped review confirmed that closure and found a transient-accept
regression. Its focused fix restores logged retry/backoff with cancellable
shutdown; three further unit regressions and the full live suite passed.
The final accept-retry patch was not sent through a third independent review.
These are scoped infrastructure reviews, not full M1 technical/style/security
certification or owner integration approval.

## Approval and source status

The owner approved execution of the complete first milestone and directed
online source acquisition using the suggested archives. That instruction is
preserved in `milestones/reference-acquisition-decision.json`; it is **not**
approval of an unseen reference pack or finished presentation.

The suggested gamepack archive ends at build 235. It was not used to downgrade
the frozen build-240 target. Original RuneLite 1.12.38 injected-client/API
artifacts were acquired as an alternative reference-tooling candidate. Their
opaque build ID does not establish the matching numeric game revision or a
working source capture session.

The calibrated visual/audio pack is not assembled or approved. Source-state
observations, complete asset closure, camera/render/interface/animation/audio
settings and per-case numeric tolerances remain work to do. Sources are
available and acquisition has begun; this checkpoint does not claim capture is
impossible or ask the owner to approve an incomplete pack.

No representative integrated-graphics hardware/browser contract was supplied
or established, and no rendered performance measurements exist. Sparky's GB10
model name alone establishes neither a GPU classification nor equivalence to
the Section 36 target. Chrome/Edge rendering, audio, source comparisons,
independent presentation reviews and final owner presentation acceptance are
all unexecuted.

## Remaining work and resume boundary

Resume **M1**, not Mining 1-99 or the Club Penguin loop. The next executable work
is recorded in `milestones/m1-tasks.json`:

1. Resolve the matching source-client/capture procedure and the exact normal
   starting inventory/flags, tutorial guards/departure branch, quest deliveries
   and first-death/recovery gaps under M1-STARTING-STATE.
2. Assemble M1-REFERENCE-PACK from concrete source inputs/captures; establish
   the representative benchmark contract, then obtain the bounded owner pack
   approval. Do not begin presentation implementation against a blank pack.
3. Build the required early real WebGPU/audio benchmark, then implement the
   full shared tutorial/Lumbridge/Cook's Assistant journey and browser/server/
   persistence paths. None of that gameplay or browser work is implemented by
   this account checkpoint.
4. Execute every remaining gate, repair failures and obtain revision-bound
   owner visual/audio acceptance before declaring M1 complete.

The account code can be reused as-is subject to future affected-gate
revalidation. Do not recreate its working protocol/persistence systems or
silently reduce the journey to make the checkpoint look complete.

## Resources and durable state

All delegated workers are parked and new admissions are closed. No worker,
factory, scheduled loop or game/test server remains running. Visible peak AI
concurrency was three including the Director, below the shared ceiling of 25.

The development container is stopped with its persistent volume and private
credential file retained. Only synthetic account data exists there. Disposable
test containers/databases/secrets were cleaned up; ports 4010 and 55432 are no
longer listening, and no `.local` credential/cache file is tracked by Git.
Unrelated host services and graphics/security settings were left untouched.

The isolated implementation worktree at `.worktrees/m1-accounts` is retained
as a parked checkpoint, not a running worker. Its commits are integrated; its
only uncommitted file is its generated task-local `Cargo.lock`. Resume from the
execution branch's committed lockfile. Original runtime artifacts and earlier
local diagnostic reports remain under ignored `.local/`.

Available accounting is recorded in
[`m1-usage-snapshot.json`](../../milestones/evidence/m1-usage-snapshot.json):
134 GPT-6 Astra API calls, 17,081,546 input and 374,184 output tokens in the
returned session snapshot, last updated at 18:36:44Z. It is delayed, not a final
or verified project-wide worker aggregate. Currency and AI-credit totals are
unavailable; the store's cost value is a billing-multiplier aggregate, not a
dollar figure. No owner AI spending cap was imposed.
