# M1 content/artifact-3 source bindings

These records accompany real generated
[`GameContent`](../../content/m1/game-content.json.gz) and its strict Runtime
[`artifact`](../../content/m1/game-content.csc.gz). The v1 representation blockers
are replaced by typed mechanics, not ignored fields or a permissive parser.

`selection.json`, `wiki-sources.json`, `wiki-facts.json`, `runtime-source-facts.json`
and `code-sources.json` retain exact source identities and evidence. Original
cache placements, wiki location rows and inferred map-radius/landing candidates
remain distinct. The 146-definition original supplement and six original NPC
records retain hashes; neither is a substitute asset pack. The current adapter
uses `publication_chain()` and `load_published_inputs()` across all three
publication layers. All72 item/68 requested model/6 NPC/13 interface roots and
the additional original potion3010/3011/model2697/placeholder19365 publication
resolve. All5257 product references close against12400 catalog records. The
272 additive assets/1137 outputs retain exact original source hashes and shard
provenance; no null asset exception or generated replacement remains.

`world-bindings.json.gz` retains all151019 source object placements, all4837
definitions and all61 source scenes. `door-bindings.json` records the actual
typed transform groups, all136 combined collision states and closed rat-pen
cells. `runtime3-selectors.json` records the nine actor-specific traversal rules,
explicit ground/combat/recovery selectors, new unarmed styles and the valid
per-character flour morph domain projection.
`spawn-bindings.json.gz` preserves stationary access policies and the rejected
mobile source candidate without relocating it or clearing its clipping.
Source player arrivals are separate from NPC pins.

`graph-bindings.json` retains all73 tutorial edges,22 Cook edges and6 source
death edges. Every required tutorial/Cook edge has actual content-3 bindings;
none remains disabled due to a v1 schema gap. `mechanics-bindings.json.gz` joins
all52 source rules and their exact arithmetic/source data to actual target IDs.
`policy-bindings.json` preserves all original assumptions and reconciliation
alternatives verbatim. Their current superseding source application is recorded
separately in `research/m1-bindings/application-result.json`; the old
currency-reset/false-auto-equip candidates are not silently used at runtime.
`profile-v2.json` retains the historical provisional choices without claiming approval.

`compiler-validation.json`, `schema-validation.json`, `check-result.json`,
`route-validation.json`, `state-oracle-validation.json`, `test-output.log` and
`repeatability.json` record qualified verification. The compiler is Runtime,
the artifact and actual source JSON are version3. `validation-run.json` records
every exit code, including the current native failures. Run
`python3 tools/m1-content/validate.py --repeat`; it must not pass while a required
native source-policy probe fails.
`unresolved-bindings.json` is the exact compiler-checked list of unresolved
paths with reasons/provenance. Unresolved values are **unavailable at runtime**,
not zeros, fixed timers, guaranteed drops or blanket source exclusions.

The parent repaired the tagged numeric-map decoder in7150464 and rebound the
actual Wind Strike table1/5/9/13=>2/4/6/8. Exact canonical application preserves
that already-applied value, applies the other103 available source replacements
and all seven coupled updates, and adds only the independently owner-approved
goblin potion event. The98 reversible inferences retain inference provenance.
Older status/hash records are historical; current manifest/application/validation
records describe the resulting artifact.

`asset-refresh-baseline.json` freezes the supplied parent artifact and its
non-asset behavior, geometry and graph hashes. `verify_assets.py` checks those
invariants, exact source catalog/publication/output hashes, requested closure
counts, resolved assets and correct collection-shard provenance. The bounded
refresh remains protected. The subsequent source application is checked against
that boundary plus its explicit target/coupled fingerprints and approved item
extension; unrelated changes are still errors.

`application-result.json` and `application-validation.json` separate active or
conditional residuals from six inactive full-target dependencies. Rare instances
must not be acquired by the proved ordinary route, fishing depletion must stay
exactly zero, and the Office proof must be rechecked on item/instance-merge changes.
No inactive unresolved value is read as a successful fallback. The actual shared
conditional vital enum is now bound and maps5/10/15 at base10->11 to5/11/15.
Mining/Cooking level gains do not need HP/Prayer policy. No active original
SourceBinding remains; that is separate from executor conformance.

Thirty source walking dependencies connect through declared combined clipping;
independent source object insertion validates636 state cells and all door states
restore exact source masks on close. All28 player travel destinations are
walkable and distinct from nonwalking NPC anchors. These are topology and
post-event/state checks, **not a full engine journey**. No renderer, raw asset, MIDI file, source
map pin or self-authored screenshot is treated as presentation/playback evidence.
Source snapshots, actual runtime execution, owner pack approval and final M1
acceptance remain separate.

`runtime3-sources.json` pins27 additional narrow public references.
`runtime3-policy.json` keeps all new supported engineering interpretations as
inferences, not observations or approvals. Fresh-account Drop behavior is
documented for the first20 played hours; the unrelated10QP GE rule does not
prove a drop-release condition. New per-origin clock/persistence requirements
are explicit, not hidden inside arbitrary flags.

The actual engine constructs from the artifact, validates fresh state and views,
executes the real appearance request and consumes source bones with45XP tenths at
two ticks. It nevertheless rejects the solid range from its valid allowed side
and fails the fresh manual-drop offline pause. Both are persisted negative
evidence, not waived checks or old v2 gaps. `contract-gaps.json` records exact
source expectations and parent code/contract actions; both content SQL tasks
remain blocked while those ordinary-path failures remain.

The original runtime-binding evidence stays read-only. Current application and
validation records live in this directory. The exact upstream audio change is
pinned in `runtime3-input-context.json`:264 current rendered files are checked,
while the prior258-file report remains historical. No audio acceptance is claimed.
