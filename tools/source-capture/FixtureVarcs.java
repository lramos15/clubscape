/** Native Varcs schema/operations, with only persisted UI-setting reads disabled for the offline fixture. */
public final class FixtureVarcs extends ds
{
    @Override
    void aj(byte guard)
    {
        // Source defaults and explicitly recorded fixture values replace persisted per-user settings.
    }
}
