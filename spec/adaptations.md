# Canonical adaptation register

The full baseline remains authoritative unless an approved scoped departure is
recorded. `prompt.md` Sections 3.2, 3.4 and 30 define the existing directional
permissions; they do not authorize arbitrary behavioral redesign or source
fidelity claims.

## adaptation.m1.goblin_energy_potion_provisional

**Owner approved.** Record:
[`milestones/m1-goblin-loot-approval.json`](../milestones/m1-goblin-loot-approval.json).

For the ordinary M1 goblin, add a separate 1-in-16 energy-potion event with
equal weights for source doses 1, 2, 3 and 4. Preserve the published mutually
exclusive 128-weight primary table, guaranteed drops and other source-bound
eligibility rules. This must not become guaranteed coins, an empty loot pool
or a members-only potion drop.

The source confirms the dose range and describes the rate qualitatively;
**1-in-16 is an approved provisional adaptation, not verified OSRS odds**.
Keep that distinction in source records, parity counts and future release
limitations. Bind the actual source item variants, ground ownership/lifetime,
event independence and replay-safe kill identity. Tests must verify all four
dose outcomes and independence from the primary selection.

No other source inference is automatically approved by this decision.
Reference-pack and final owner presentation acceptance remain separate gates.
