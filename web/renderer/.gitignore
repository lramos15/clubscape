# Generated by `cargo build --target wasm32-unknown-unknown` + wasm-bindgen (see README.md); the
# TypeScript ABI declarations stay committed so the shell can integrate without a Rust toolchain.
/pkg/*.js
/pkg/*.wasm
/dist/
